import React from 'react';
import ReactDOM from 'react-dom';
import { {{ name }} } from './component/Component';

ReactDOM.render(
  <React.StrictMode>
    <{{ name }} />
  </React.StrictMode>,
  document.getElementById('root')
);
